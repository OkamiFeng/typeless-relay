name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: make test
      - name: Build installer artifacts
        run: make package VERSION=0.0.0
